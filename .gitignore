# Ce dépôt est PUBLIC : tout ce qui y entre est visible de tous.
# Les photos d'origine envoyées par l'artisan restent sur l'ordinateur
# et ne partent pas en ligne. Seules les versions préparées pour le site,
# dans assets/photos/, sont publiées.
photos stephane chantier/

# Fichiers créés par le Finder
.DS_Store
